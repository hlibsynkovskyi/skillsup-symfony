{% extends 'base.html.twig' %}

{% block body %}
    {% if order.paidIsSuccess %}
      <i>Спасибо за покупку!</i>
    {% endif %}

    {% if order.paidIfFail %}
        <i>Извините, оплата не успешнаю Попробуйте еще раз.</i>
    {% endif %}

    {% if order.paidIsUnknown %}
        <i>Извините, данные об оплате еще в пути. Подождите...</i>

        <script type="text/javascript">
            setTimeout(function() {location.reload()}, 20000);
        </script>
    {% endif %}

{% endblock %}
